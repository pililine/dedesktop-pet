name: Build desktop installers

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: read

env:
  APP_DIR: apps/desktop-pet

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-14
            bundle_args: "--bundles app,dmg"
            artifact_name: desktop-pet-macos-aarch64
          - platform: windows-latest
            bundle_args: "--bundles nsis"
            artifact_name: desktop-pet-windows-x64

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/desktop-pet/package-lock.json

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop-pet/src-tauri

      - name: Install dependencies
        working-directory: apps/desktop-pet
        run: npm ci

      - name: Frontend checks
        working-directory: apps/desktop-pet
        run: |
          npm run typecheck
          npm run test
          npm run lint
          npm run format:check
          npm run build

      - name: Rust checks
        working-directory: apps/desktop-pet/src-tauri
        run: |
          cargo fmt --check
          cargo test
          cargo clippy -- -D warnings

      - name: Build platform bundle
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: apps/desktop-pet
          args: ${{ matrix.bundle_args }}

      - name: Upload installers
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}-${{ github.sha }}
          if-no-files-found: error
          path: |
            apps/desktop-pet/src-tauri/target/release/bundle/dmg/*.dmg
            apps/desktop-pet/src-tauri/target/release/bundle/macos/*.app
            apps/desktop-pet/src-tauri/target/release/bundle/nsis/*.exe
